import React, { useState } from 'react';
import { 
  ShieldCheck, 
  ShieldAlert, 
  ArrowUpRight, 
  ArrowDownLeft, 
  Trash2, 
  Settings2,
  Power,
  Activity,
  Plus
} from 'lucide-react';

/**
 * Single-file Firewall Automation Tool
 * Features: Rule mapping, status toggling, and directional iconography.
 */
const FirewallAutomation = () => {
  // Mock data representing firewall rules usually fetched via API
  const [rules, setRules] = useState([
    { id: 1, name: 'Web Server HTTPS', port: '443', protocol: 'TCP', direction: 'inbound', active: true, hits: '1.2k' },
    { id: 2, name: 'SSH Management', port: '22', protocol: 'TCP', direction: 'inbound', active: false, hits: '0' },
    { id: 3, name: 'Database Egress', port: '5432', protocol: 'TCP', direction: 'outbound', active: true, hits: '850' },
  ]);

  const toggleRule = (id) => {
    setRules(rules.map(rule => 
      rule.id === id ? { ...rule, active: !rule.active } : rule
    ));
  };

  const deleteRule = (id) => {
    setRules(rules.filter(rule => rule.id !== id));
  };

  return (
    <div className="max-w-3xl mx-auto p-6 bg-slate-50 min-h-screen font-sans text-slate-900">
      {/* Header Section */}
      <header className="flex items-center justify-between mb-8">
        <div>
          <h1 className="text-2xl font-bold tracking-tight">NetShield Automator</h1>
          <p className="text-slate-500 text-sm italic">Automated Traffic Filtering & Policy Control</p>
        </div>
        <button className="flex items-center gap-2 bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg font-medium transition-all shadow-sm">
          <Plus size={18} /> New Rule
        </button>
      </header>

      {/* Stats Overview */}
      <div className="grid grid-cols-3 gap-4 mb-8">
        <div className="bg-white p-4 rounded-xl border border-slate-200 shadow-sm">
          <p className="text-xs text-slate-400 uppercase font-bold">Active Rules</p>
          <p className="text-2xl font-black">{rules.filter(r => r.active).length}</p>
        </div>
        <div className="bg-white p-4 rounded-xl border border-slate-200 shadow-sm">
          <p className="text-xs text-slate-400 uppercase font-bold">Total Traffic</p>
          <p className="text-2xl font-black text-indigo-600">2.05k <span className="text-sm font-normal text-slate-400">hits</span></p>
        </div>
        <div className="bg-white p-4 rounded-xl border border-slate-200 shadow-sm">
          <p className="text-xs text-slate-400 uppercase font-bold">System Status</p>
          <p className="text-2xl font-black text-green-500 underline decoration-2 underline-offset-4">Secure</p>
        </div>
      </div>

      {/* Rules List (Mapping) */}
      <div className="space-y-3">
        {rules.map((rule) => (
          <div 
            key={rule.id}
            className={`flex items-center justify-between p-4 rounded-xl border transition-all duration-300 ${
              rule.active ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-100 border-transparent opacity-60'
            }`}
          >
            <div className="flex items-center gap-4">
              {/* Security Icon */}
              <div className={`p-2 rounded-lg ${rule.active ? 'bg-green-100 text-green-600' : 'bg-slate-300 text-slate-600'}`}>
                {rule.active ? <ShieldCheck size={24} /> : <ShieldAlert size={24} />}
              </div>

              {/* Logic Details */}
              <div>
                <div className="flex items-center gap-2">
                  <h3 className="font-bold text-slate-800">{rule.name}</h3>
                  <span className="text-[10px] bg-slate-800 text-white px-1.5 py-0.5 rounded font-mono uppercase">
                    {rule.protocol}
                  </span>
                </div>
                <div className="flex gap-4 mt-1 text-xs text-slate-500 font-medium">
                  <span className="flex items-center gap-1">
                    {rule.direction === 'inbound' ? 
                      <ArrowDownLeft size={14} className="text-blue-500" /> : 
                      <ArrowUpRight size={14} className="text-orange-500" />
                    }
                    {rule.direction.charAt(0).toUpperCase() + rule.direction.slice(1)}
                  </span>
                  <span>Port: {rule.port}</span>
                  <span className="flex items-center gap-1"><Activity size={12}/> {rule.hits} Hits</span>
                </div>
              </div>
            </div>

            {/* Automation Controls */}
            <div className="flex items-center gap-2">
              <button 
                onClick={() => toggleRule(rule.id)}
                className={`p-2 rounded-lg transition-colors ${
                  rule.active ? 'text-green-600 hover:bg-green-50' : 'text-slate-500 hover:bg-slate-200'
                }`}
                title="Toggle Activation"
              >
                <Power size={20} />
              </button>
              <button className="p-2 text-slate-400 hover:text-indigo-600 hover:bg-indigo-50 rounded-lg transition-colors">
                <Settings2 size={20} />
              </button>
              <button 
                onClick={() => deleteRule(rule.id)}
                className="p-2 text-slate-400 hover:text-red-600 hover:bg-red-50 rounded-lg transition-colors"
              >
                <Trash2 size={20} />
              </button>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};

export default FirewallAutomation;